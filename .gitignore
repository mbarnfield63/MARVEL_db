# data + secrets — everything under data/ except the vendored refs
/data/*
!/data/refs/
.env
*.env
# python
__pycache__/
*.pyc
.venv/
# postgres volume
/pgdata/
graphify-out/
.claude/
# ingestion manifests — local working files, not checked in
/manifests/by-publication/*.yaml
