# Copy to .env (gitignored). Compose reads .env; the loader reads DATABASE_URL.
POSTGRES_USER=marvel
POSTGRES_PASSWORD=marvel
POSTGRES_DB=marvel
POSTGRES_PORT=5432

DATABASE_URL=postgresql://marvel:marvel@localhost:5432/marvel
