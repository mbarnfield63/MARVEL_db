# data/refs/

`marvel.bib` goes here — the vendored MARVEL section of the ExoMol BibTeX,
copied from `C:\Code\bib\MARVEL\`. The loader resolves every
`publications.bibtex_key` against this file and **hard-fails** on a missing
key, so keep the MARVEL section complete. Sync manually when new papers land.
